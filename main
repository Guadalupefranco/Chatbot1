#Chatbot elaborado por Team ZUGUS
from functions import *
#Se crea el chatbot  
bot = create_bot('Segubot')
#Entrenamos al chatbot con los datos 
train_all_data(bot)
#Agregamos los datos de nuestro chatbot 
house_owner = [
]
custom_train(bot, house_owner)
#inicializamos nuestro chatbot
start_chatbot(bot)
